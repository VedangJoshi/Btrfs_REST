/*
 *
 *  This module lets a user make give call to the Content-based-storage module
 *	
 *	pip install Flask
 *	pip install flask-restful
 *	sudo apt-get install python3-dev
 *
 *  Server-side :-
 *	python sample-rest.py 
 * 
 * 	Client-side :-
 *	dd if=/dev/urandom of=file1.bin bs=64M count=16
 *	dd if=/dev/urandom of=file2.bin bs=64M count=16
 * 	curl -H "Content-type: application/octet-stream" -X POST http://127.0.0.1:5000/aaa --data-binary @file1.bin
 *	curl -H "Content-type: application/octet-stream" -X POST http://127.0.0.1:5000/aaa --data-binary @file2.bin
 *	
 *
 */



