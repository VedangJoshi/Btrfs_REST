/*
 *
 *      This module lets a user(test.py) to make a call to a C function using the 
 *	SWIG library.
 *	
 *	sudo apt-get install python-dev
 *	sudo apt-get install python3-dev
 * 	make
 *	python test.py
 *	
 *	########################################################################
 *		We'll use this to convert the REST call to the IOCTL call for the 
 *	content based storage part  of our system. 
 *	########################################################################
 *
 */
